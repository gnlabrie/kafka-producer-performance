spring:
  profiles:
    active: local
  config:
    import:
      - optional:file:./config/profiles/application-local.yaml
      - optional:file:./config/security/plaintext.yaml
      - optional:file:./config/scenarios/variable-1k-1m-5minutes.yaml

kafka-producer-test:
  run:
    name: local-plaintext
    description: Local PLAINTEXT fixed-payload smoke workload
