kafka-producer-test:
  security:
    mode: SASL_SSL_GSSAPI
    gssapi-service-name: kafka
    gssapi-login-mode: TICKET_CACHE
    gssapi-use-ticket-cache: true
    truststore-path: ${TRUSTSTORE_PATH}
    truststore-password: ${TRUSTSTORE_PASSWORD}
